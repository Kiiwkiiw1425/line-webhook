{
  "name": "dpis-line-bot-webhook",
  "version": "1.0.0",
  "description": "LINE Bot webhook handler for DPIS manual",
  "main": "line_webhook_handler.js",
  "scripts": {
    "start": "node line_webhook_handler.js",
    "test": "echo \"Error: no test specified\" && exit 1"
  },
  "dependencies": {
    "@line/bot-sdk": "^7.5.0",
    "express": "^4.18.2",
    "fuse.js": "^6.6.2"
  },
  "engines": {
    "node": ">=18.0.0"
  }
}
